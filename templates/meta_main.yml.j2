---
## Generated file. Do not edit directly.
## Source template: {{ template_name }}
dependencies: []
galaxy_info:
  role_name: openssh
  namespace: guidugli
  author: guidugli
  company: Creuzo Tech
  description: >-
    Install and configure OpenSSH server settings, apply hardening-oriented
    defaults, and keep release metadata aligned with the shared Molecule
    platform matrix.
  license: MIT
  min_ansible_version: '2.14'
  platforms:
{% for platform in platforms %}
    - name: {{ platform.name }}
      versions:
{%   for version in platform.versions %}
        - {{ version | tojson }}
{%   endfor %}
{% endfor %}
  galaxy_tags:
    - openssh
    - ssh
    - linux
    - security
    - hardening
    - server
