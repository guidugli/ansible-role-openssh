---
- name: Restart sshd
  ansible.builtin.service:
    name: "{{ openssh_service_name }}"
    enabled: true
    state: restarted
  when: ansible_facts['service_mgr'] == 'systemd'
  tags:
    - openssh
    - service
